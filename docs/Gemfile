# For local preview:  cd docs && bundle install && bundle exec jekyll serve
# CI uses actions/jekyll-build-pages, which brings its own bundle.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-remote-theme"
gem "webrick"
